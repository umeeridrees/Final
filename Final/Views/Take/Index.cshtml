@model IEnumerable<Final.Models.Take>

@{
    ViewBag.Title = "Index";
}

<h2>Results</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.marks)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.rollno)
        </th>
        <th>
            Test Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.marks)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.rollno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Test.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.take_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.take_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.take_id })
        </td>
    </tr>
}

</table>
